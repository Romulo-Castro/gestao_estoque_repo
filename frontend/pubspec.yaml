name: frontend
description: "Aplicativo de gestão de estoques"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  cupertino_icons: ^1.0.2
  camera: ^0.10.0
  image_picker: ^1.0.0
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  mime: ^1.0.5
  http_parser: ^4.1.2
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  flutter_secure_storage: ^9.2.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
